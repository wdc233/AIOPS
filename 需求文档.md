【项目目标】
实现一个基于 LangChain 的智能运维 Agent 系统，支持通过指令总线动态下发巡检任务，具备心跳自检、定时 Cron、即时执行三种调度模式，同时支持用户交互式指令输入和智能意图识别。要求 7×24 小时稳定运行，支持多 Agent 分布式部署。

【核心架构要求】

1. 分层架构（严格遵循）：
   - Control Plane（控制平面）：指令总线 + 任务持久化 + 流编排 + 审计日志
   - Agent Runtime（执行引擎）：LangChain ReAct Agent + 工具链 + LLM 意图识别子 Agent
   - Tool Layer（工具层）：SSH、命令执行、日志分析、Prometheus/Grafana API、LLM 趋势预测、告警 Webhook
   - Environment Layer（环境层）：全局集群信息管理，支持 Agent 自主决策

2. 关键设计模式（必须实现）：
   - 指令总线：StarRocks 持久化 + 发布订阅，支持指令状态机（pending→running→completed/failed）
   - 会话隔离：每个巡检任务独立 Session，用户交互会话保持上下文
   - 车道锁（Lane Lock）：同一目标服务器串行执行，防止并发 SSH 风暴
   - 心跳机制：轻量级自检任务，每 30 分钟执行一次，使用低成本推理
   - 双模调度：心跳驱动 + Cron 定时，Cron 触发完整 ReAct 循环
   - 意图识别子 Agent：专门处理用户自然语言输入，多轮对话补全信息
   - 审计日志：全链路操作记录，写入 StarRocks 审计表

【技术栈】
- 框架：LangChain + LangGraph（用于 ReAct 循环和意图识别子 Agent 编排）
- 数据库：StarRocks（使用 pymysql 或 starrocks-python-client 异步封装）
- 通信：WebSocket 或 SSE 用于实时指令推送和用户交互
- 并发：asyncio，车道锁用 asyncio.Lock
- 配置：Pydantic Settings，支持环境变量
- 监控 API：Prometheus HTTP API、Grafana API
- LLM：支持 OpenAI/Claude/本地模型，用于趋势预测和意图识别
【LangChain 具体实现要求】

1. Agent 结构：
   - 主 Agent：使用 LangGraph 构建 StateGraph，节点：observe → analyze → predict → decide → act → report
   - 意图识别子 Agent：独立 StateGraph，节点：intent_parse → slot_check → tool_select → confirm
   - 工具（Tools）必须包含：
     * SSHCommandTool：在远程服务器执行命令，返回结构化结果
     * LogAnalysisTool：分析日志文件，支持正则和异常检测
     * PrometheusQueryTool：查询 Prometheus 指标，支持 PromQL
     * GrafanaQueryTool：查询 Grafana 仪表盘和数据源
     * TrendPredictionTool：基于历史指标数据，使用 LLM 或统计模型预测趋势风险
     * AlertWebhookTool：调用已有告警 Webhook URL 发送预警
     * EnvironmentQueryTool：查询全局环境信息，获取集群/服务器元数据
   - 记忆：用户交互会话使用 ConversationBufferMemory，巡检任务会话隔离

2. 数据模型（Pydantic）：

   ```python
   # 巡检指令模型
   class InspectionCommand(BaseModel):
       command_id: str
       action: Literal["schedule", "run_now", "cancel", "update"]
       name: str
       cron: Optional[str]
       targets: List[str]
       inspection_items: List[InspectionItem]
       session_type: Literal["isolated", "persistent"] = "isolated"
       callback_url: Optional[str] = None
       priority: int = 1
       created_by: Optional[str] = None  # 用户或 system
   
   # 用户交互意图模型
   class UserIntent(BaseModel):
       intent_type: Literal["query_metric", "check_status", "run_inspection", "unknown"]
       target_cluster: Optional[str] = None
       target_ip: Optional[str] = None
       metric_name: Optional[str] = None
       time_range: Optional[str] = None
       missing_slots: List[str] = []  # 缺失的信息槽位
       confidence: float = 0.0
   
   # 全局环境信息模型
   class ClusterInfo(BaseModel):
       cluster_name: str
       cluster_type: Literal["k8s", "vm", "baremetal"]
       servers: List[ServerInfo]
       prometheus_url: Optional[str] = None
       grafana_url: Optional[str] = None
       labels: Dict[str, str] = {}
   
   class ServerInfo(BaseModel):
       ip: str
       port: int = 22
       username: str
       password: Optional[str] = None  # 加密存储，使用密钥管理服务
       private_key: Optional[str] = None
       os_type: Literal["linux", "windows"] = "linux"
       role: List[str] = []  # web, db, cache 等
       cluster_name: str
   
   # 审计日志模型
   class AuditLog(BaseModel):
       log_id: str
       timestamp: datetime
       operation_type: Literal["command_publish", "inspection_run", "user_query", "tool_call", "alert_send"]
       operator: str  # 用户ID或 agent_id
       target: Optional[str] = None
       input_data: Dict
       output_data: Dict
       status: Literal["success", "failure"]
       error_msg: Optional[str] = None
       duration_ms: int

【代码质量要求】
全异步实现（async/await），StarRocks 使用异步连接池（aiomysql 或自研异步客户端）
完整类型注解（mypy 检查通过）
结构化日志（structlog 或标准 logging + JSON 格式）
异常处理：所有异常必须捕获并记录，关键错误上报 Sentry（预留接口）
配置热加载：修改配置无需重启 Agent
优雅关闭：SIGTERM 信号处理，完成当前任务后退出
数据库连接：使用连接池，禁止泄露，所有查询参数化防止 SQL 注入

【交付标准】
可运行的完整代码，包含 main.py 入口
包含测试用例：
发布定时巡检指令，验证 StarRocks 持久化和审计日志
用户交互：输入"查看 CPU"，验证多轮追问和意图识别
Prometheus 查询 + LLM 趋势预测，验证风险预警发送
并发场景下车道锁验证
提供 Dockerfile 和 docker-compose.yml，包含 StarRocks 服务
README 包含架构图（Mermaid）和快速开始指南
【禁止事项】
不要使用 LangChain 的 deprecated API（如 LLMChain）
不要在 Agent 中使用阻塞的 time.sleep() 或同步 HTTP 请求
不要把数据库连接泄露到全局，必须使用连接池或上下文管理器
不要硬编码密钥，所有敏感配置走环境变量或密钥管理服务
不要明文存储密码，必须加密
【参考设计】
借鉴 OpenClaw 的 SQLite-backed 任务账本和车道设计
借鉴 XXL-JOB 的分布式调度思想，但保持 Agent 自治心跳
意图识别参考 Rasa 或 Dialogflow 的槽位填充机制